@model IEnumerable<CategoryDto>

@{
    ViewData["Title"] = "Categories List";
    //Layout = "~/Views/Shared/_layout.cshtml";
}

<h1>@ViewBag.PageTitle</h1>
@* <table class="table table-hover table-striped">
    <thead>
        <th>Category Name</th>
        <th>Category Description</th>
    </thead>
    <tbody>
        @foreach (var cat in Model)
        {
            <tr><span>@cat.CategoryName</span></tr>
            <tr><span>@cat.Description</span></tr>
        }
    </tbody>
    
</table> *@

<table class="table table-hover table-striped">
    <thead>
        <tr>
            <th>Category Name</th>
            <th>Category Drescription</th>
            <th>Related Product</th>
        </tr>
    </thead>
    <tbody>
        @foreach (CategoryDto cat in Model)
        {
            <tr>
                <td><span>@cat.CategoryName</span></td>
                <td><span>@cat.Description</span></td>
                <td><a class="btn btn-info" as asp-area="Products" asp-controller="Home" asp-action="CategoryWiseProducts" asp-route-id="@cat.CategoryId">Show Related Products</a></td>
            </tr>
        }
    </tbody>
</table>
